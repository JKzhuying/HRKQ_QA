# v8.6.5 发行说明 - 文件签署中心升级

## 概述
本版本将原有的"知情同意书"功能全面升级为"文件签署中心"，支持4种牙科治疗类型的同意书管理，新增种植体标签照片导入功能，并修复了多项关键问题。

---

## 新增功能

### 1. 文件签署中心（通用同意书引擎）
- **4种同意书类型支持**：种植手术、补牙、拔牙、根管治疗
- **模板驱动架构**：每种类型独立的条款内容和表单字段，通过数据库模板动态渲染
- **同意书列表管理**：分页展示、按类型筛选、关键字搜索
- **新建/编辑/作废**：完整的CRUD操作，支持草稿保存和修改

### 2. 条款内容管理
- **4套完整条款**：
  - 种植手术：7条核心条款（含品牌/型号/牙位变量）
  - 补牙：30条条款（含风险告知、注意事项、患者选择）
  - 拔牙：20条条款（含病史确认、拔牙注意事项）
  - 根管治疗：24条条款（含12条治疗说明、手写区域）
- **条款变量自动替换**：表单填写内容自动填充到条款中的占位符
- **模板自动更新**：每次启动服务自动同步代码中的最新条款到数据库

### 3. 动态表单字段
- **字段类型**：文本输入(text)、数值输入(number)、牙位选择(tooth_selector)、单选(yesno)、手写(handwrite)
- **按类型自动渲染**：根据同意书类型自动显示对应字段
- **必填/选填控制**：每种字段可配置是否必填

### 4. FDI 牙位选择器
- **交互式牙位图**：上颌/下颌各16颗牙位按钮
- **FDI标准编号**：11-18（右上到左上）、21-28、31-38、41-48
- **多选支持**：可选择多个牙位，实时显示已选牙位名称

### 5. 手写区域支持
- **补牙**：治疗方案（医生手写）
- **拔牙**：建议项（患者拒绝签字区域）
- **根管治疗**：主诉、PE、患者陈述、医生陈述
- **实时保存**：手写内容自动保存为PNG图片

### 6. 电子签名系统
- **三方签名**：患者签名、亲属签名（可选，含关系选择）、主治医生签名
- **HTML5 Canvas 手写**：支持鼠标和触摸屏
- **防误操作**：签名前需先保存同意书
- **签名预览**：保存后实时显示签名缩略图

### 7. 种植体标签照片导入（v8.6.5 新增）
- **仅种植手术同意书可用**
- **拍照/上传**：点击上传区域选择图片或拍照
- **图片规格**：JPG/PNG，最大 10MB
- **A4 PDF排版**：照片在PDF中A4宽度90%（约170mm）等比缩放，显示在页面底部签名区域下方
- **自动替换**：上传新照片自动替换旧照片

### 8. PDF 生成与下载
- **一键归档**：签名完成后点击"完成归档"自动生成PDF
- **A4标准格式**：包含完整条款、手写内容、签名、标签照片
- **中文字体自动适配**：支持WQY ZenHei/Noto CJK等字体
- **归档锁定**：PDF生成后记录锁定，不可再修改

---

## 修复的问题

| 问题 | 修复说明 |
|------|---------|
| JS语法错误导致页面无法打开 | 修复 `_renderConsentFields` 中字符串拼接转义错误 |
| 条款内容不显示 | 新增模板自动刷新机制，确保数据正确写入数据库 |
| 编号显示 "undefined" | 后端返回字段 `doc_no` 与前端的 `consent_no` 字段名不一致 |
| 牙位选择按钮无响应 | 修复 `onclick` 参数传递，正确包裹引号 |
| 手写签名区域不可用 | 同上，修复字符串拼接语法 |
| 根管治疗NO.字段多余 | 移除NO.输入框，自动使用文档编号填充 |
| 数据库模板数据为空 | 改为无条件UPDATE，确保启动时数据同步 |

---

## 数据库变更

### 新增表
- `consent_templates`：同意书模板（类型、标题、条款、字段定义）
- `consent_documents`：通用同意书（替代原 `informed_consents`）

### 新增字段
- `consent_documents.barcode_image_path`：种植体标签照片路径

### 数据迁移
- 自动从 `informed_consents` 迁移历史数据到 `consent_documents`

---

## 技术依赖变更

| 新增依赖 | 版本 | 用途 |
|---------|------|------|
| reportlab | 4.2.0 | PDF生成 |
| Pillow | 10.3.0 | 图片尺寸读取（PDF排版） |

---

## 文件变更清单

### 后端
- `backend/database.py` - 新增同意书表、模板表、字段迁移
- `backend/routes/consents.py` - 通用同意书引擎（重写）
- `backend/app.py` - 注册 consents 蓝图
- `backend/requirements.txt` - 新增 reportlab、Pillow

### 前端
- `backend/static/index.html` - 文件签署中心界面
- `backend/static/js/app.js` - 完整前端逻辑
- `backend/static/css/style.css` - 样式调整

### 其他
- `consent_templates_json.sql` - 模板数据初始化SQL
- `RELEASE_NOTES_v8.6.5.md` - 本发行说明

---

## 部署说明

```bash
# 1. 上传代码到服务器
# 2. 安装新依赖
pip install reportlab==4.2.0 Pillow==10.3.0

# 3. 重启 Flask 服务（必须重启，代码中有启动时初始化逻辑）
# 4. 刷新浏览器页面
# 5. 如需手动初始化模板数据，在 phpMyAdmin 执行 consent_templates_json.sql
```

---

## 版本信息
- **版本号**：v8.6.5
- **发布日期**：2026-08-03
- **上一个版本**：v8.5.5
