# 口腔诊所财务管理系统 v8.6.0 全面代码审查报告

**审查日期**: 2026-07-31
**审查版本**: v8.6.0 (从v8.5.5升级)
**审查目的**: 确保生产环境可以安全覆盖更新，不影响已有业务数据

---

## 一、审查结论

**总体评价**: 代码可以安全覆盖更新，但需注意以下关键事项。

**覆盖前必须做的操作**:
1. 备份 `backend/.config/config.enc` (数据库加密配置)
2. 备份 `backend/uploads/` 目录 (上传的照片、签名、PDF等)
3. 服务器安装中文字体: `apt-get install -y fonts-wqy-zenhei`
4. 安装新依赖: `pip install reportlab==4.2.0`

---

## 二、文件清单 (26个文件)

### 2.1 新增文件 (1个)
| 文件 | 说明 | 安全 |
|------|------|------|
| `backend/routes/consents.py` | 知情同意书模块 (620行) | 新增表不影响旧数据 |

### 2.2 修改文件 (5个)
| 文件 | 变更内容 | 影响评估 |
|------|---------|---------|
| `backend/app.py` | 注册consents蓝图 (+1行) | 安全 |
| `backend/database.py` | 新增informed_consents表 + 迁移逻辑 (+64行) | 安全(CREATE IF NOT EXISTS) |
| `backend/static/index.html` | 知情同意书页面 + 预览弹窗 + 牙位选择 + 签名板 (+~200行) | 安全 |
| `backend/static/js/app.js` | 知情同意书交互逻辑 (+~350行) | 安全 |
| `backend/requirements.txt` | 新增reportlab==4.2.0 | 需安装 |

### 2.3 未修改文件 (20个)
所有旧功能模块保持不变，包括:
- routes/transactions.py, routes/vouchers.py, routes/bank_accounts.py 等
- utils/config_manager.py, utils/excel_parser.py
- backend/wsgi.py, backend/gunicorn_conf.py

---

## 三、数据库影响分析

### 3.1 新增表 (1个) - 完全安全
```sql
informed_consents -- 知情同意书表，不影响任何已有表
```

### 3.2 表迁移逻辑 (自动执行) - 安全
`database.py` 中的 `_migrate_consents_table()` 函数:
- 使用 `ALTER TABLE ADD COLUMN` 逐个添加字段
- 所有操作包裹在 try/except 中，字段已存在时静默忽略
- **不会删除、修改或影响已有数据**

### 3.3 init_database() 调用时机
| 入口点 | 调用条件 | 说明 |
|--------|---------|------|
| `app.py` (Flask应用级) | `_is_installed()` 为真 | 应用启动时执行 |
| `wsgi.py` (uWSGI入口) | `config_exists() and get_db_config()` | 多worker时每个都会尝试 |
| `start.py` (开发入口) | 同上 | 仅开发使用 |

**风险**: `wsgi.py` 中多worker会同时调用 `init_database()`，但所有SQL使用 `IF NOT EXISTS` 或 try/except，不会造成问题。

---

## 四、高风险操作清单

### 4.1 数据清空功能 (已有功能，非新增)
以下API会清空数据，**正常业务操作不会触发**，但需要知晓:

| 接口 | 功能 | 风险等级 |
|------|------|---------|
| `POST /api/transactions/clear` | 清空income_records + expense_records | 高 |
| `POST /api/vouchers/clear-all` | 清空vouchers + voucher_entries | 高 |
| `POST /api/settings/restore` | 恢复备份时清空所有业务表再INSERT | 高 |

### 4.2 表删除操作 (仅在特定条件下)
| 位置 | 条件 | 说明 |
|------|------|------|
| `database.py:311` `_drop_if_exists('income_items')` | 表存在时 | 旧表清理，只影响空表 |
| `database.py:322` `DROP TABLE transactions` | 数据已迁移后 | 旧表迁移完成后删除 |
| `database.py:267-268` `DROP TABLE voucher_entries/vouchers` | 有外键约束时重建 | 仅vouchers表 |

### 4.3 用户删除 (安装向导)
| 位置 | 条件 | 说明 |
|------|------|------|
| `setup.py:134-135` `DROP USER` | 安装时创建用户前 | 删除的是随机生成的用户名，不影响系统 |

---

## 五、备份表完整性

### 5.1 问题: 自动备份表列表缺失 (已修复)
**原问题**: `auto_backup.py` 的 `_BACKUP_TABLES` 列表缺少库存管理和知情同意书表

**修复后完整列表**:
```python
_BACKUP_TABLES = [
    # 业务核心表
    'income_records', 'expense_records', 'transfer_records',
    # 基础数据表
    'category_records', 'bank_accounts',
    'account_subjects_l1', 'account_subjects_l2', 'category_subject_map',
    # 凭证表
    'vouchers', 'voucher_entries',
    # 财务报表相关
    'accounting_periods', 'subject_balances', 'opening_balance_skip_log',
    # 系统配置表
    'system_settings', 'auto_backup_settings', 'auto_backup_logs',
    # 库存管理表 (v8.5)
    'inventory_photos', 'suppliers', 'supplier_photos',
    'inventory_records', 'inventory_logs', 'inventory_batch_counters',
    # 知情同意书表 (v8.5.5)
    'informed_consents',
]
```

---

## 六、前端ID冲突问题 (已修复)

### 6.1 问题描述
知情同意书预览弹窗中的 `id="preview-count"` 与Excel导入预览的 `span id="preview-count"` 冲突，导致种植体数目显示到错误的DOM元素。

### 6.2 修复
- HTML: `id="preview-count"` → `id="preview-implant-count"`
- JS: `document.getElementById('preview-count')` → `document.getElementById('preview-implant-count')`

---

## 七、生产环境部署检查清单

### 7.1 覆盖前备份 (必须)
```bash
# 1. 备份数据库配置
cp /www/wwwroot/dental-finance/backend/.config/config.enc /tmp/config.enc.backup

# 2. 备份上传文件
cp -r /www/wwwroot/dental-finance/backend/uploads /tmp/uploads.backup

# 3. 备份数据库 (可选但推荐)
mysqldump -u root -p dental_finance > /tmp/dental_finance_backup.sql
```

### 7.2 安装依赖 (必须)
```bash
# 进入虚拟环境
source /www/server/pyporject_evn/versions/3.10.20/bin/activate

# 安装reportlab
pip install reportlab==4.2.0

# 安装中文字体 (解决PDF中文方块)
apt-get install -y fonts-wqy-zenhei
```

### 7.3 覆盖文件
```bash
# 解压新版本到临时目录
unzip HRKQ_QA_v860.zip -d /tmp/new_version

# 覆盖所有文件 (保留.config和uploads)
cp -r /tmp/new_version/HRKQ_QA_git/* /www/wwwroot/dental-finance/

# 确保配置和上传文件还在
ls /www/wwwroot/dental-finance/backend/.config/config.enc
ls /www/wwwroot/dental-finance/backend/uploads/
```

### 7.4 重启服务
```bash
# 重启uWSGI
touch /www/wwwroot/dental-finance/backend/app.py
# 或
pkill -f uwsgi
```

### 7.5 验证
```bash
# 检查日志
tail -f /www/wwwroot/dental-finance/logs/uwsgi.log

# 访问系统确认正常
# 测试知情同意书功能
```

---

## 八、版本变更摘要

| 版本 | 变更 |
|------|------|
| v8.5.5 | 库存管理模块 (入库/供应商/预警) |
| v8.6.0 | 新增知情同意书电子签名功能 |

**v8.6.0 核心功能**:
- 种植手术知情同意书新建/编辑
- FDI牙位选择 (CSS十字相位法)
- Canvas电子签名 (患者/亲属/医生)
- A4纸张大小预览弹窗
- reportlab PDF生成 (嵌入签名图片)
- 完成归档流程
